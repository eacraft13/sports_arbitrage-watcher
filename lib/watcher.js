var r = require('rethinkdb');




module.exports = function(conn) {

    // r.table('lines').changes().run(conn)
    // .then(function(cursor) {
    //     return cursor.toArray();
    // })
    // .then(function(results) {
    //
    // });

};
